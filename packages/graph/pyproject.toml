[build-system]
requires = ["hatchling>=1.24.0"]
build-backend = "hatchling.build"

[project]
name = "skripts-graph"
version = "0.1.0"
description = "Graph orchestration package"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
skripts-graph = "graph.main:run"

[tool.hatch.build.targets.wheel]
packages = ["graph"]
