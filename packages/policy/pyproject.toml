[build-system]
requires = ["hatchling>=1.24.0"]
build-backend = "hatchling.build"

[project]
name = "skripts-policy"
version = "0.1.0"
description = "Policy engine package"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
skripts-policy = "policy.main:run"

[tool.hatch.build.targets.wheel]
packages = ["policy"]
